#include <stdio.h>
#include "main.h"

/**
 * _abs - Check description
 * @n: An input number
 * Description: gives the absolute value of input number
 * Return: Absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (r * -1);
}
