#include "main.h"

/**
 * main - Entry point
 * @argc: argument count
 * @argv: arguments to run program
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
