#include "main.h"

/**
 * main - Entry point
 * @argc: argument count
 * @argv: arguments to run program
 * Return: 0 (Success)
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
