#include <stdio.h>

/**
 * main - prints run program name
 * @argc: number of arguments in command line
 * @argv: arguments passed to program
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
